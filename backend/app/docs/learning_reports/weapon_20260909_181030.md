# Weapon detector — 20260909_181030

Base `yolo26n.pt`, dataset YouTube-GDD (gun class only), trained on Google Colab
(T4), 60 epochs, imgsz 640, batch 32.

| metric | value |
|---|---|
| precision | 0.870 |
| recall | 0.654 |
| mAP@50 | 0.751 |
| mAP@50-95 | 0.584 |

Dataset: 4000 train (682 hard negatives) / 387 val / 500 test held out.

Installed at `models/weapon_detector.pt`; `models/weapon_registry.json` updated.
The server loads it on start when `weapon.enabled: true` (config.yaml).

Read: precision 0.87 = few false gun boxes (good — a false Critical is expensive).
recall 0.65 = it will miss ~1 in 3 guns, mostly small / distant / odd-angle —
the held-gun association + `hold_frames` vote in `src/weapon.py` trade a little
more recall for precision on top of this. Raise `weapon.min_conf` if you still
see false Criticals; lower it (or drop `hold_frames` to 2) if it misses obvious
held guns. Retrain with more data (Roboflow, operator-confirmed crops from the
LEARNING panel) to lift recall. See docs/WEAPON_DETECTION.md.
